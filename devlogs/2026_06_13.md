## Planned

* code the full CRUD api with djangorestframework
* test
* start building html views with templates
* authentication deliberately left for later coding
* ExternalPlaceIdentity deliberately left out for now
* tests through CLI as well as Django's admin page

## Change of plans

* set up superuser
* set up authentication first, because most of the app is based on a user-layer
* leave out ExternalPlaceIdentity for now
* set up proper API, with accounts -> API tokens -> CRUD access to their layer
* test if authentication works properly, as well as logging in
* test if constraints and relations are set up properly

## Finished

* created Restaurants' Django REST Framework ViewSet
* tested with httpie
* superuser set up
* authentication set up
* CRUD works for Restaurant as well as URProfile
* URProfiles are separated between users, with no access to other users' URProfiles
* set up proper error message on URProfiles unique constraint
* extensive testing with Django REST Framework's browsable APIjj
