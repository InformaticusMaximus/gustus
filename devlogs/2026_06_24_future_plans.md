## Notes:
* School MVP has been turned in and I've decided to grow the project significantly.
* I will probably use open source APIs instead of Google Places for cost control.
* Working with dirtier data will also teach me a lot about cleaning it.
* The aim for now becomes a refactor, cleaning and organising some code I've rushed.

## Planned:
* Rethink pipelines and possible branching out of the data pipeline.
* Organize directories. The HTML page code should be in the main core/ directory, while REST API should be seperated into an api/ directory, because it is not the primary focus.
* Frontend would be better represented with a list display instead of currently used boxes.
* It is time to connect external place identities for further development to take place.
* PyTest should definitely be implemented at this point in development.
* Dish during visit as well as cuisine type for each restaurant will be needed for a good recommendation algorithm.

## Vision as of now
**Gustus needs to function as a standalone history-storing and recommendation system without overly relying on external data.**
The eventual implementation of data analysing algorithms should come naturally after enough development.
I definitely do not want to create a broken app which showcases copying ready algorithms from the internet and bending the planned structure to fit that.
I intend to build a good structure first, do my research about recommendations and implement algorithms adequate to Gustus.
